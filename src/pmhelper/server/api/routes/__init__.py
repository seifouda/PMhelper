"""
API Routes Module

REST API endpoints for PMHelper server functionality.
Modules are imported directly where needed (main.py) to avoid
pulling in desktop-only dependencies during web-only deploys.
"""
