# Render.com Infrastructure as Code
services:
  - type: web
    name: pmhelper-edu
    runtime: docker
    plan: free
    branch: EDU_PROD
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DEBUG
        value: "false"
      - key: ENVIRONMENT
        value: production
    healthCheckPath: /health
